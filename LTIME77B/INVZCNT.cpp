#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

void solve () {
    int N, Q;
    scanf ("%d %d", &N, &Q);
    vector < int > A (N);
    for (int & x : A) scanf ("%d", & x);
    
}

int main() {
    int T;
    scanf ("%d", &T);
    while (T--) {
        solve();
    }
    return 0;
}
